spring:
  config:
    activate:
      on-profile: "s3"
  servlet:
    multipart:
      maxFileSize: 10MB
      maxRequestSize: 10MB

#AWS
cloud:
  aws:
    s3:
      bucket: ${S3_BUCKET:default}
    credentials:
      access-key: ${S3_ACCESS_KEY:default}
      secret-key: ${S3_SECRET_KEY:default}
    region:
      static: ${S3_REGION:ap-northeast-2}
    stack:
      auto: false
